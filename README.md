# RCS TECH WORLD React

Run locally:
```bash
npm install
npm run dev
```

Production:
```bash
npm run build
```

The app uses the published Google Sheet CSV, search/filter, live refresh, WhatsApp enquiry, image zoom and responsive dark UI.
